RED.causeway.outliner = (function () {
    var treeList;
    var flowList;
    function getPathsData() {
        var pathData = [{
            label: "Flows",
            expanded: true,
            children: []
        }]
        flowList = pathData[0]
        return pathData
    }
    function build() {
        var container = $("<div>", { class: "red-ui-info-outline" }).css({
            height: "100%",
        });
    }
    // api to return
    return {
    };
})();
