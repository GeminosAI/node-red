var util = require("@node-red/util").util;
var runtime;

var api = module.exports = {
    init: function(){

    },
    set: async function(nodeID) {
        return new Promise(function (resolve, reject) {
            try {
                _node = runtime.nodes.getNode(nodeID);
                console.log(_node)
                if (_node) {
                    resolve()
                }
                console.log("GEMINOS causeway.js runtime: IN HERE ")

            } catch (exception) {
                console.log(exception)
                reject(exception)
                return exception
            }
        });
    }
}